[project]
name = "tp-welding"
version = "0.1.0"
description = "TP Welding Quality Detection Challenge"
dependencies = [
    "pydantic",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
